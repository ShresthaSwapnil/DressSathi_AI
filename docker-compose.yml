services:
  api:
    build: ./server
    env_file: ./server/.env
    ports:
      - "8000:8000"
    restart: unless-stopped
    volumes:
      - dressmate_uploads:/app/uploads

volumes:
  dressmate_uploads:
